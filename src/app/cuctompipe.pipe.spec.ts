import { CuctompipePipe } from './cuctompipe.pipe';

describe('CuctompipePipe', () => {
  it('create an instance', () => {
    const pipe = new CuctompipePipe();
    expect(pipe).toBeTruthy();
  });
});
